import { AddressSecretPair } from "../lib/address-secret-pair";

describe("AddressSecretPair#constructor", function() {
  
  context("when no secret seed is provided", function() {
    it("initialized the seed randomly");
  }); 
   
  context("when a secret seed is provided", function() {
    it("uses the provided seed");
  });

});